var trustOracle = artifacts.require("TrustOracle");
var lockOracle = artifacts.require("LockOracle");
var alice = artifacts.require("BaseContractCentralized");
var bob = artifacts.require("BaseContractCentralized");
var carl = artifacts.require("BaseContractCentralized");

module.exports = function (deployer) {
    // deployer.deploy(alice, trustOracle.address, lockOracle.address);
    // deployer.deploy(bob, trustOracle.address, lockOracle.address);
    // deployer.deploy(carl, trustOracle.address, lockOracle.address);
};
