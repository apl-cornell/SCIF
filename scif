#!/bin/bash

ROOT=`dirname $0`
GRADLEW_NAME="gradlew"
GRADLEW=${ROOT}/$GRADLEW_NAME
ARGUMENTS=\""${array[*]}"\"
echo $ARGUMENTS
COMMAND="bash "${GRADLEW}" run --args=${ARGUMENTS} --quiet" 2> /dev/null

echo ${COMMAND}
${COMMAND}
