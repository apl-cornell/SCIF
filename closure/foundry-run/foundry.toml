[profile.default]
src = "src"
test = "test"
out = "out"
libs = []
# One config for every number in the evaluation chapter: the same solc,
# optimizer on (production practice, and what the SCIF paper's sizes imply).
optimizer = true
optimizer_runs = 200
via_ir = true
