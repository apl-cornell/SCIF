a:int{Alice}


def foo{Alice}(a:int{Alice}) -> int{Alice}:
    if a == 1 :
        return bar(a + 1)
    else :
        i : int{Alice} = a
        rnt : int = 0
        while i > 0 :
            --i
            rnt = rnt + i
        return rnt

x : int{Bob} = 1
def bar{Alice}(a:int{Alice}) -> int{Alice} :
    return endorse(x, Bob, Alice)
