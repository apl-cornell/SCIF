import "implC.scif";

interface B {
    @public void bar{sender->sender;this}();
}

contract A {
    B addr;
	C cddr;
    constructor() { super(); }

    @public void foo() {
        addr.bar();
    }

	@public void fooo() {
    	cddr.car();
    }
}
