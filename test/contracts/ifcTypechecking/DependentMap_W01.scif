
contract DependentMap {
    // name as a principal; name ⨆ this
    map(address name, uint{name}) balances;


    @public
    void{y} foo{y}(final address{TOP} y, uint{BOT} amount) {
        balances[y] = amount;
    }

    @public
    void bar{sender}() {
//        foo(sender);
    }
}