import "./ILockManager.scif";
import "./ITrustManager.scif";

interface IBaseContract {
    @public
    bool directlyTrusts(address trustee);
    @public
    address[] directTrustees();

    @public
    ITrustManager trustManager();
    @public
    ILockManager lockManager();
}
